name: Dependency Review

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  dependency-review:
    uses: cuioss/cuioss-organization/.github/workflows/reusable-dependency-review.yml@072cef7145e9735ace4f1f40bcaf07ef86258b0a # v0.3.0
    permissions:
      contents: read
      pull-requests: write
