name: Dependency Review

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  dependency-review:
    uses: cuioss/cuioss-organization/.github/workflows/reusable-dependency-review.yml@b0fe583e68bf7a835718da45ba426157d620bc8f # v0.2.4
    permissions:
      contents: read
      pull-requests: write
