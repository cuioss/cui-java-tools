name: Dependency Review

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  dependency-review:
    uses: cuioss/cuioss-organization/.github/workflows/reusable-dependency-review.yml@2569924aa94ecef006ab4c96db06f43b051e5dbd # v0.6.5
    permissions:
      contents: read
      pull-requests: write
