name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '27 4 * * 3'

permissions:
  contents: read

jobs:
  analyze:
    # Centralized in cuioss-organization, matching every other security/CI workflow in this repo.
    # The reusable workflow fixes the query suite to +security-and-quality org-wide;
    # build-mode: none analyses the sources directly (no Maven build needed for this
    # zero-dependency library).
    uses: cuioss/cuioss-organization/.github/workflows/reusable-codeql.yml@19d5894332b91eb52396659568933c4a751346a4 # v0.14.1
    permissions:
      security-events: write
      contents: read
      actions: read
    with:
      languages: '["java"]'
      build-mode: none
